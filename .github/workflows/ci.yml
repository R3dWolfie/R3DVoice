name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @r3dvoice/server prisma generate
      - run: pnpm --filter @r3dvoice/server prisma migrate deploy
        env:
          DATABASE_URL: "file:./dev.db"
      - run: pnpm --filter @r3dvoice/shared build
      - run: pnpm -r typecheck
      - run: pnpm --filter @r3dvoice/server test
        # Note: vitest.config.ts injects its own env block for tests, which
        # overrides any env we set here. This block is only for the migrate
        # step above. Keep vitest.config.ts in sync if you change secrets.
