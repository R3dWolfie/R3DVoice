# Copy to apps/server/.env and fill in real values.
DATABASE_URL="file:./dev.db"

# Generate: `openssl rand -base64 32`
JWT_SECRET="replace-me-min-32-chars-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

# LiveKit — defaults here match infra/livekit.yaml for local dev.
# In production, generate fresh keys and update both files.
LIVEKIT_URL="ws://localhost:7880"
LIVEKIT_API_KEY="devkey-r3dvoice"
LIVEKIT_API_SECRET="devsecret-r3dvoice-devsecret-r3dvoice-32"

# Master key for at-rest encryption of TOTP secrets + room chat bodies.
# >=32 chars. If unset, those columns are stored plaintext (dev mode).
# Generate: `openssl rand -base64 48`
# Once set, NEVER change it on a database with existing encrypted data —
# the wrapped values become unreadable.
MASTER_KEY="replace-me-min-32-chars-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
